services:
  networkhub:
    build: .
    container_name: networkhub
    ports:
      - "3000:3000"
    volumes:
      - networkhub-data:/app/data
    environment:
      - PORT=3000
      - NODE_ENV=production
      - DB_PATH=/app/data/data.db
    restart: unless-stopped

volumes:
  networkhub-data:
